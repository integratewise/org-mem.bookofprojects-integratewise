# Frontend / Brand Runtime — Brand Documentations
use mise

# Repo-local Node tooling
export PATH="$PWD/node_modules/.bin:$PATH"

# Per-repo env
dotenv_if_exists .env.local
dotenv_if_exists .env

# Repo identity
export REPO_NAME="brand-documentations"
